# Changelog

All notable changes to this project are documented here, starting from this release. Format loosely follows [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

This cycle accumulated several breaking renames (settings prefix, filter field names,
owner-filter shortcuts, and the full phase/state terminology pass below) — see
[docs/upgrade-checklist.md](docs/upgrade-checklist.md) for a single actionable
checklist covering all of them, with `grep` commands to find every call site in a
consumer project.

### Added

- **[GitHub issue #3](https://github.com/sigfrido/workflango/issues/3)** — the demo project's Supplier and Request list pages now show both the generic workflow filters (phase, owner, suspended, unread, message, date range — via `WorkflowFilter`/`WorkflowFilterForm`) and custom per-model filters: Request by minimum budget and supplier; Supplier by name, tax code, and whether it has requests (`testproject/demo/filters.py`'s new `SupplierFilter`/`RequestFilter`, wired into `SupplierListView`/`RequestListView.get_queryset()`)
- `WF_CUSTOM_OWNER_FILTERS` setting — lets a consuming project register its own "Owner" filter shortcuts (e.g. an "Away" status, or a "My own direction" filter keyed off its own User model) without patching workflango, replacing the removed `-7`/"Away" builtin with a generic, documented extension point instead. `{id: (label, callable)}`, `callable(lookup, request) -> Q`; see README.md "Owner filter shortcuts". A misbehaving callable surfaces as a normal filter error (`BaseFilter.filter_queryset()` already wraps query-building in a try/except), not a crash
- `docs/wf-configuration.md` — full reference for every key `configure_workflow()`/`WorkflowConfig` recognizes at every level (global kwargs, per-phase, per-transition inside `reachable_phases`, and the `properties` sub-dict), with allowed values and defaults. Previously only a terse example existed (the module docstring at the top of `wf_config.py`); several real keys (`caption`, `owner_mode`, `allowed_groups`, `hidden`, `disabled`, `require_message`, `default_owner`, `allow-reject`, and the `properties` keys `edit_button_label`/`help_topic`/`description`/`disable_editing`) were used throughout the engine and templates but never documented anywhere. Linked from README.md's "Quick start". Also documents the container/deployment init order (`migrate` → `check_wf_config` → `init_wf_groups`) and clarifies that `WF_USERS_GROUPS` only declares valid group names — `init_wf_groups` is what actually creates the Django `Group` rows
- `WorkflowConfig.check()` (`manage.py check_wf_config`) now also validates that every model has at least one terminal phase (`is_closed: True`) — new `check_has_terminal_phase()`, called from `check()`. A workflow with no terminal phase means an instance can never reach a final state, which is essentially always a configuration mistake; previously this went undetected until it accidentally became relevant much later

### Changed

- All Django settings renamed to a unified `WF_` prefix: `WORKFLOW_USERS_GROUPS` → `WF_USERS_GROUPS`, `WORKFLOW_ADMIN` → `WF_ADMIN`, `WORKFLOW_ADMIN_GROUP` → `WF_ADMIN_GROUP`, `WORKFLOW_TRANS_MSG_MAX_LEN` → `WF_TRANS_MSG_MAX_LEN`, `WORKFLANGO_ALLOW_IMPERSONATE` → `WF_ALLOW_IMPERSONATE`, `WORKFLANGO_SNAPSHOT_ENABLED` → `WF_SNAPSHOT_ENABLED`, `WORKFLANGO_ACCESS_DENIED_URL` → `WF_ACCESS_DENIED_URL`, `WORKFLANGO_NOTIFY_FUNC` → `WF_NOTIFY_FUNC`. `WF_HISTORY_TRANS_TYPE` (added this cycle) already had the correct prefix. **Breaking for projects upgrading from `1.0.0rc1`**: update `settings.py` before restarting. (Three README.md examples still showed the pre-rename names — `WORKFLANGO_ALLOW_IMPERSONATE`, `WORKFLANGO_SNAPSHOT_ENABLED`, and a `WORKFLOW_ADMIN_GROUP` mention — fixed.)
- `USER_CHOICES`'s six builtin "Owner" filter shortcuts renamed from negative-integer strings to descriptive ones, for consistency with the new `WF_CUSTOM_OWNER_FILTERS` keys: `-1` (Me) → `me`, `-2` (Me or none) → `me_or_none`, `-3` (None) → `none`, `-4` (Someone) → `someone`, `-5` (Not me) → `not_me`, `-6` (Not active) → `not_active`. Behavior of each is unchanged, only the dispatch key in `filter_by_owner()` — there was never an `int()`/sign check anywhere relying on the old negative-number convention, it existed only to avoid colliding with real (positive) user ids, which a non-numeric string key achieves just as well. **Breaking**: a `search_wf_proprietario=-1`-style querystring value now silently matches nothing (falls through to the `owner__id=-1` fallback) instead of applying the "Me" filter — same category of pre-1.0 rename as `WF_*`/`search_wf_*` above
- The `search_wf_*` query param names were only prefix-renamed in an earlier commit; the field name itself was still Italian (`WorkflowFilter.search_fields` keys, `WorkflowFilterForm` field names, `WorkflowFilterSetMixin.declared_filters` keys, and the shipped `workflow_filters.html` fragment). Renamed to English throughout, matching each field's already-English label: `search_wf_fase` → `search_wf_phase`, `search_wf_messaggio` → `search_wf_message`, `search_wf_sospeso` → `search_wf_suspended`, `search_wf_da_leggere` → `search_wf_unread`, `search_wf_proprietario` → `search_wf_owner`, `search_wf_data_min`/`_max` → `search_wf_date_min`/`_max`, `search_wf_stato_old` → `search_wf_history` (`search_following` was already English, left as-is). **Breaking**: any existing querystring/bookmark using the old names stops filtering (same pre-1.0 rename category as above). Incidentally, `WorkflowFilter.post_process_search_fields()`'s dead `'search_fase' in sfdict` check (a leftover from before *any* of these renames, matching neither the old nor the new key, so it never actually fired) now correctly reads `'search_wf_phase'` and populates that field's `choices` with the model's phase list, as it was always meant to
- **Full "Phase vs State" terminology pass across the entire library**, following the rule *"Phase is the class, State is the instance"*: a `Phase` is the category (a static string name defined in `WorkflowConfig`), a `State` is the Django model instance recording one point in an object's transition history — an object **is in** a `State`, which **belongs to** a `Phase`. This went well beyond the filter/form layer already fixed above, reaching into the core engine, which predated even an earlier partial "state→phase" rename from before this release. Two consumer-facing touch points are **breaking**:
  - The `nuovo_stato` URL kwarg (used by `ChangeStateView`'s URL pattern and `WorkflowModel.get_change_state_url()`) → `destination_phase`. Any project's own `urls.py` copied from the README's documented pattern (`path('.../<str:nuovo_stato>/', ChangeStateView.as_view(...))`) must update its path converter to match. `ChangeStateView`/`ChangeStateForm` class names, the `change_state`/`change_state_form` action names, and the `*_change_state` URL *name* pattern all stay as-is — "state" there is colloquial UX language ("change the record's status"), not a reference to the `State` model.
  - The `'reachable_states'` key used in every `workflow_phases`/`configure_workflow()` config tuple → `'reachable_phases'`. Any project's own workflow config (not just `testproject/demo`) must update this key.

  Everything else is an internal rename, still worth knowing about if you imported or overrode any of these directly: `WorkflowConfig.get_state_config`→`get_phase_config`, `.get_states_list`→`.get_phases_list`, `.get_state_order`→`.get_phase_order`, `.editors_for_state`→`.editors_for_phase`, `.get_candidate_users_for_state`→`.get_candidate_users_for_phase`, `.get_states_for_permissions`→`.get_phases_for_permissions`, `.check_unreachable_states`→`.check_unreachable_phases`, `.reach_states_from`→`.reach_phases_from`; `InstanceWorkflowManager.transition()`/`.transition_allowed()`'s `new_state` parameter → `new_phase`; `.reject_to_state()`→`.reject_to_phase()`; `.get_transition()`'s `dest_state`→`dest_phase`; `State.get_previous_different_state()`→`.get_previous_different_phase()`; `State.wfm_state_config()`→`.wfm_phase_config()`; `State.get_state_order()`→`.get_phase_order()`; `State.get_state_property()`→`.get_phase_property()`; `WorkflowModel.current_state_str()`→`.current_phase_str()`; `WorkflowModel.get_candidate_users()`'s `for_state`→`for_phase` parameter (incidentally fixing a latent bug: its default fallback passed a real `State` *instance* into a phase-keyed dict lookup, which would have raised `KeyError` had the default path ever been exercised); `WFTransitionDescriptor.__init__()`'s `dest_state`→`dest_phase`; `.state_property()`/`.state_help_topic`/`.state_description`→`.phase_property()`/`.phase_help_topic`/`.phase_description`; `.get_destination_state_property()`→`.get_destination_phase_property()`; `InvalidState` exception → `InvalidPhase`; the undocumented `'validate_state_transition'` consumer hook → `'validate_phase_transition'`. `WorkflowConfig`'s own `State.phase_str()` (deliberately dual-purpose) and `after_state_transition` (its `previous_state` parameter is a genuine `State` instance) are unchanged, correctly named already under the same rule. `workflango.mixins_tests`' shipped `GUITestMixin`/`WorkflowTestMixin` test helpers (`transition()`, `transition_allowed()`, `ok_transition()`, `get_change_state_view()`, `post_change_state_view()`) also had their `new_state`/`state` parameters renamed to `new_phase`/`phase` — breaking for any downstream consumer's test suite calling them by keyword.
- Demo project: `Supplier.EDITABLE_FIELDS_BY_PHASE`/`Request.EDITABLE_FIELDS_BY_PHASE` (an ad hoc class-level dict, outside the `workflow_phases` config entirely) migrated into each phase's own `'properties': {'editable_fields': {...}}`, read via `Supplier.wfm_config.get_phase_config(phase)['properties'].get('editable_fields', set())` in `SupplierForm`/`RequestForm`. A real-world worked example of `properties`' documented "any other key is a pure pass-through bag for your own app" extension point (see `docs/wf-configuration.md`, section 3) — this key is read only by the demo's own forms, not by workflango itself. Only `testproject/`, no library behavior change

### Fixed

- `WorkflowViewSetMixin.check_wf_permission` (DRF) did not pass `impersonated_by` to `is_owner()`, so any state created during an impersonation session (`State.impersonated_by != None`) raised `PermissionDenied` for two valid callers: the admin-impersonating-owner session that created it, and the real owner reclaiming it afterwards without impersonation. `check_wf_permission` now takes `impersonated_by` and passes it to `is_owner()`; a second guard (`state.owner == acting_user`) lets the real owner through when `is_owner()` returns False due to impersonation-context mismatch, so `transition_allowed()`'s existing `reclaiming_own_identity` logic can do its job. Three regression tests added to `tests/tests.py` (`CheckWfPermissionImpersonationRegressionTest`)
- `check_wf_permission(self, instance, acting_user)` read `impersonated_by = getattr(self.request, 'impersonated_by', None)` internally — a convention only ever populated by a GUI-style session-swap middleware. A pure DRF caller impersonating via `change_state`'s `"user": <id>` body param never sets `request.impersonated_by`, so this coarse pre-filter always evaluated ownership as if no impersonation were happening; the `state.owner == acting_user` fallback papered over the common case (impersonating the object's *current* owner), but a legitimate impersonated action that wasn't yet the current owner could be wrongly rejected with 403 before ever reaching `transition_allowed()` (which already received the correct value). `check_wf_permission` now takes `impersonated_by` as an explicit third parameter (`impersonated_by=None`) instead of reading it implicitly; both call sites (`WorkflowViewSetMixin.change_state`, `SebastianWorkflowViewSetMixin.change_state_form`) updated to pass the value they already have. Source-compatible for existing 2-arg callers; a subclass that relied on the old implicit `self.request.impersonated_by` read must now pass it explicitly
- **Security hardening**: `resolve_acting_user()`/`get_effective_user()` (DRF) trusted `request.user` unconditionally when checking `get_impersonable_users()`. If a consuming project combines a session-swapping middleware (like the demo's `ImpersonateMiddleware`) in front of a DRF `WorkflowViewSetMixin` endpoint, `request.user` as seen by these methods would already be the impersonated target, not the real caller — so authorization for a *further* impersonation would be checked against the impersonated identity's own delegation rights instead of the real caller's ("user1 acting as user2 impersonating user3"). Not exploitable in this repo today (`testproject` never combines the two), but nothing prevented it. Both methods now anchor every authorization check to `request.impersonated_by` when already set, falling back to `request.user` only when it isn't — an *anti-chaining invariant*: impersonation can never chain, no matter how many layers reassign `request.user` upstream. `get_effective_user()`'s plain-call early return also no longer silently drops an already-set `impersonated_by` to `None`. Four regression tests added (`tests/tests.py`'s `ResolveActingUserAntiChainingTest`); see README.md "API authorization and anti-chaining" and `docs/wf-configuration.md`'s `impersonable_users` entry for the consumer-facing writeup, including a worked `hr_app` service-account example
- `SebastianWorkflowSerializerMixin.datetime_format` renamed to `wf_datetime_format` to avoid clashing with `GUISerializerMixin.datetime_format` (drf-sebastian) which is a strftime pattern used in `_render_datetime()`. When both mixins were combined (the typical usage), `datetime_format = 'SHORT_DATETIME_FORMAT'` propagated to sebastian's `_render_datetime()`, which called `dt.strftime('SHORT_DATETIME_FORMAT')` — returning the literal string instead of a formatted date, since the value contains no `%` codes. The workflango attribute is now `wf_datetime_format` (a Django format-variable name resolved via `django.utils.formats.date_format`); consuming projects that overrode `datetime_format` on `SebastianWorkflowSerializerMixin` subclasses to change the state-badge date format must rename the override to `wf_datetime_format`
- `WorkflowFilterForm` (`forms.py`) and the shipped `workflango/django/workflow_filters.html` fragment still used pre-rename field names (`search_fase`, `search_proprietario`, ...) after `WorkflowFilter.search_fields` was renamed to the `search_wf_*` prefix in an earlier commit — the form/template were entirely unusable for driving `WorkflowFilter`, since the query params they produced didn't match any key `WorkflowFilter` reads. Both renamed to match; `WorkflowFilterBackend`'s docstring (DRF) updated too. Demonstrated working end-to-end by GitHub issue #3's demo filter bars, which are the first thing to actually wire this form up
- Every field on `WorkflowFilterForm` defaulted to Django's `required=True`, so the browser's HTML5 validation blocked submission unless every filter was explicitly touched -- including `search_wf_stato_old`, whose first, pre-selected choice is `('', 'Current')`: an empty *value* the browser treats as "no selection" on a required field, so the form couldn't even be submitted with its own default left alone. All fields now `required=False`, as a search/filter form's fields must be (an unfilled field means "don't filter on this", not "pick something first"). This doesn't change what an unfilled vs. an explicitly-selected-`None` value means -- e.g. leaving Owner blank still means "no owner filter", distinct from explicitly picking owner "None" (`-3` in `USER_CHOICES`, meaning "owner is null") -- `BaseFilter.build_query()` already only skips a field when its GET value is entirely absent or `''`
- `check_wf_config`/`check_wf_objects` declared a stale Django-1.x-style `args = '[<appname.model>, ...]'` class attribute, which modern argparse-based management commands never consult -- `add_arguments()` never declared a matching positional, so `manage.py check_wf_config myapp.MyModel` failed with `error: unrecognized arguments: myapp.MyModel` instead of scoping the check to that model. Both commands now declare a proper `models` positional (`nargs='*'`, defaulting to every `WorkflowModel` subclass when omitted, same as before). Regression tests added to `testproject/demo/tests.py` (`ManagementCommandTests`)

### Removed

- `USER_CHOICES`'s `-7`/"Away" owner-filter shortcut (`filter_by_owner()` in `filters.py`), which hardcoded the lookup `owner__user_config__away` -- a relation specific to WebGPV's own User model, not present on a generic Django user model, so selecting it raised `FieldError: Unsupported lookup 'user_config__away' for ForeignKey or join on the field not permitted` for every other consumer. A generic hook for consumer-defined owner-filter shortcuts (so a project can add its own "Away"-equivalent without patching workflango) is being evaluated separately

## [1.0.0rc1] - 2026-08-27

First release candidate. Feature-complete and used in production (as the `workflow` app inside WebGPV, at Comune di Milano since 2014); this candidate exists to get more real-world mileage as a standalone package before committing to the API-stability guarantee of a full `1.0.0`.

### Added

- Full Django i18n support for the library's own GUI chrome, with a bundled Italian translation catalog. `{% wtrans %}` (template) and `wgettext()`/`wgettext_lazy()` (Python) wrap the required `msgctxt "workflango"` so call sites don't need to write it by hand — see README.md "Internationalization (i18n)" for the maintainer workflow, including the `_translatable_strings.py` extraction registry `makemessages` needs
- `WorkflowModel.view_base_name`, `get_edit_url()`, `get_history_url()`, `get_change_state_url(destination)` — a single, formalized URL-naming convention (`<view_base_name>_list/_detail/_edit/_history/_change_state`) that the shipped templates and `GUITestMixin`'s view helpers now consistently rely on, replacing an unreconciled mix of an ad hoc `get_absolute_url()` derivation and an undocumented `view_base_name` attribute `GUITestMixin` already assumed existed
- Demo project (`testproject/`, English throughout): a traditional Django project (plain class-based views, no REST framework) driving two related models with group-based permissions — `Supplier` (`proposed` → `active` → `archived`, activation/archival gated to a `MANAGERS` group) and `Request` (`draft` → `submitted` → `approved`/`rejected`, restricted to targeting active suppliers only), plus a phase-conditional edit-form template pattern standing in for the field-level permissions workflango's group-based model doesn't have. Role-based demo accounts (`create_demo_data` management command, also seeding a few sample suppliers/requests) and GUI/workflow smoke tests (`testproject/demo/tests.py`) using `GUITestMixin`
- Demo GUI for the library's impersonation feature, superuser-only: `demo/middleware.py`'s `ImpersonateMiddleware` swaps `request.user` for a session-selected target (`ImpersonateStartView`/`ImpersonateStopView`, `demo/forms.py`'s `ImpersonateForm`) and stashes the real admin on `request.impersonated_by` — exactly the contract `_BaseWorkflowTransitionMixin.check_and_transition()` already expected, so transitions performed while impersonating record `State.impersonated_by` correctly with no library changes. Navbar shows the real admin's name with the impersonated target in parentheses, or a start link when not impersonating
- Full API reference generated with `pdoc` (`docs/api/`, regenerate with `tools/gen-docs.py`)

### Changed

- README rewritten with a "Django GUI" section documenting the view/URL conventions, an "Internationalization" section, and a "Demo project" section with the accounts table
- `workflango/templates/workflango/django/*.html` — every hardcoded Italian string wrapped for translation; every string in `TRANS_TYPE_MAP`, `views.py`'s `command_captions`, `views_mixins.py`'s runtime/access-denied messages, `forms.py`, `filters.py`, `filtersets.py`, and `wf_transition.py`'s transition-caption fallback converted from hardcoded Italian to English source strings wrapped with `wgettext()`/`wgettext_lazy()`
- Management commands' `help` text and `drf.py`'s `PermissionDenied` messages and sebastian GUI field/action labels (previously hardcoded Italian, never reaching any translation catalog) translated to English source strings, the latter now also wrapped for translation
- Demo `Supplier`/`Request` `workflow_defaults` now grant `read` broadly (`USERS` and `MANAGERS`) regardless of phase — only `edit`/`admin` (who can transition an object) stay narrowed per phase. Previously `read` defaulted to empty and only ever came as a side effect of also holding `edit`/`admin` *at that specific phase*, so e.g. a plain user could never view a supplier once it reached `active`/`archived`, and a manager couldn't view one they'd just archived (`archived` has no `edit`/`admin` group at all)
- `drf.py`'s direct coupling to sibling project [drf-sebastian](https://github.com/sigfrido/drf-sebastian) — the `gui_field`-decorated list column, `__can_update`/`to_representation()`, `template_namespace`, `can_update()`/`can_delete()`, `get_workflow_transitions()`, and the `history`/`change_state_form` actions — moved out into a new optional module, `workflango.contrib.sebastian` (`SebastianWorkflowSerializerMixin`/`SebastianWorkflowViewSetMixin`), never imported by core workflango. `workflango.drf` no longer imports or references sebastian at all; a consumer using drf-sebastian as their GUI layer now opts in explicitly via `workflango.contrib.sebastian` instead of getting it bundled unconditionally. **Breaking for consumers relying on the old bundled behavior** (e.g. SELCO) — see [docs/sebastian-integration.md](docs/sebastian-integration.md) for the drop-in replacement mixins

### Fixed

- `workflango/templates/workflango/django/*.html` never actually rendered since the WebGPV→workflango rename: `{% load bootstrap %}` / `{% load dashboard_tags %}` / `{% filter gpvref %}` (none exist in workflango) and context variables `workflow_view`/`edit_view`/`history_view`/`help_page` that nothing in the library ever set. `ChangeStateView.template_name` / `ObjectHistoryView.template_name` also pointed at `workflow/...` paths that didn't match the templates' real location under `workflango/django/...`
- `ChangeStateView` crashed with `AttributeError: 'NoneType' object has no attribute 'get_absolute_url'` on every successful transition when no `success_url` was set: `ModelFormMixin.form_valid()` sets `self.object = form.save()`, and `ChangeStateForm.save()` is a no-op returning `None`, so the default `get_success_url()`'s `self.object.get_absolute_url()` crashed before `_BaseWorkflowTransitionMixin`'s own `self.object` recovery ever ran. `ChangeStateView.get_success_url()` now uses `get_object()` (cached) instead of the transient `self.object`
- Demo `Request` detail/edit/change-state/history pages showed no transition buttons and an edit-button caption of the literal text "None": naming a model `Request` collides with Django's default `context_object_name` (the lowercased model name), so `SingleObjectMixin` was overwriting the real `HttpRequest` in the template context with the `Request` *instance* — every `{% if request.user == ... %}` in the shipped workflow templates silently came up false. Fixed with an explicit `context_object_name` on the affected demo views (`_RequestObjectNameMixin`); the missing `edit_button_label` (a separate, unrelated bug) is now set in both demo models' `workflow_defaults`
- Demo `Request`'s `draft` phase granted `MANAGERS` no `admin` rights (unlike `Supplier`'s parallel `proposed` phase, which does), so a manager viewing a draft request they didn't own never saw a "Take ownership (ADMIN)" button — an asymmetry in the demo config, not a library bug
- **[GitHub issue #1](https://github.com/sigfrido/workflango/issues/1)** — impersonation was audited only as a side-effect of an actual `transition()` call (`State.impersonated_by`), but every ownership check (`is_owner()`, and the inline equivalent inside `transition_allowed()`) compared only `state.owner == user`. Since the library's documented contract has consumers swap `request.user` to the impersonated target, an admin impersonating userx satisfied every ownership check exactly as if they *were* userx — with no forced audit trail and no distinction from userx acting for themselves. Worse, plain field edits (`WorkflowModelUpdate`) never create a `State` row at all, so an impersonated plain edit left zero trace. New `State.owned_by(user, impersonated_by=None)` requires both the raw owner match *and* an impersonation-context match; `is_owner()`, `take_ownership()`, `can_take_ownership()`, and `transition_allowed()` (which now also receives `impersonated_by` at all — it previously didn't, so no fix to the above could ever have taken effect) all route through it. An admin impersonating a user (or that user reclaiming back afterwards) must now go through an explicit, audited `take_ownership()` call before `is_owner()`-gated actions succeed — via a new `user == new_owner == current_owner` ("reclaiming own identity") rule in `transition_allowed()`'s reassignment check, which works identically regardless of group membership on either side, so it doesn't depend on the impersonated target being a manager. Also fixes a related `take_ownership()` bug where reclaiming a record already recorded as owned by the target silently no-opped, dropping the passed `impersonated_by` even when explicitly given. Two new `State.transition_type` values, `impersonate` and `reclaim`, make the hand-off explicit in the history instead of being mislabeled `resume`; a different, separately-authorized impersonator taking over an existing claim (e.g. a second admin) is by design (both claims independently audited as `impersonate`) — workflango deliberately doesn't gate *who* may impersonate *whom*, only that the currently-acting identity matches what's recorded, see README.md "Impersonation" / "Ownership and impersonation"
- `workflow_buttons.html` (and five other shipped templates) still gated action buttons with a raw `state.owner == request.user` comparison, unlike the `wf_editable`/`allowed_transitions` context variables the fix above already made impersonation-aware — so Release/Suspend/Delegate (and a few purely cosmetic bits: the "Me" label, unread-button visibility, list-view highlighting) kept rendering for an admin impersonating the owner *before* their explicit reclaim, even though clicking them correctly failed at `transition_allowed()` time. New `{% load workflow_tags %}` filter `owned_by` (`{% if state|owned_by:request %}`) wraps `State.owned_by()` for template use; every such comparison across the shipped templates now goes through it
- **[GitHub issue #2](https://github.com/sigfrido/workflango/issues/2)** — three date/datetime formatting call sites hardcoded Italian-style `DD/MM/YYYY` regardless of the active locale, a holdover from before this release's i18n work: `SebastianWorkflowSerializerMixin.datetime_format` (a raw `%d/%m/%Y %H:%M` strftime literal), `WorkflowFilterForm.search_data_min`/`search_data_max`'s `input_formats`, and `filters.datestr_local2iso()`'s accepted formats. All three now go through `django.utils.formats` instead, so the accepted/displayed format follows the active locale (verified: under `it` the output is unchanged from before, e.g. `"28/08/2026 15:45"`; under `en` it correctly becomes `"08/28/2026 3:45 p.m."` instead of staying Italian). `datetime_format` is now a Django format-variable *name* (default `'SHORT_DATETIME_FORMAT'`), not a strftime pattern — overriding it requires Django's own format-string syntax (e.g. `'d/m/Y H:i'`), not Python's `%d/%m/%Y`. Incidentally fixed a real, locale-independent bug found along the way: `search_data_min`/`search_data_max`'s `DateInput` widget had `type: 'date'` (an HTML5 native date input, which requires its `value` in ISO `YYYY-MM-DD` regardless of display locale) paired with `format='%d/%m/%Y'`, which could prevent the native date picker from correctly pre-filling a bound form's value — now hardcoded to the correct `'%Y-%m-%d'`
